import {Typography} from "@material-ui/core";

export const options = {
    /*
    overrides: {
        ...(["h1", "h2", "h3", "h4", "h5", "h6"].reduce((result, item) => {
            result[item] = {
                component: Typography,
                props: {
                    variant: item,
                    component: item
                }
            };
            return result;
        }, {})),
        p: {
            component: Typography,
            props: {
                variant: 'body1',
                component: 'p',
            },
        }
    }
     */
}
